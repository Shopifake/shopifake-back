meta {
  name: Delete Category
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/categories/{{categoryId}}
  body: none
  auth: none
}

vars:pre-request {
  baseUrl: http://localhost:8081
}

script:post-response {
  bru.setEnvVar("categoryId", "");
}

docs {
  # Delete Category

  Deletes the currently selected category. The request fails if the category is still referenced by any product.
}

