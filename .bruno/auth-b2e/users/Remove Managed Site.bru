meta {
  name: Remove Managed Site
  type: http
  seq: 7
}

delete {
  url: {{host}}/api/auth-b2e/users/me/sites/:siteId
  body: none
  auth: none
}

params:path {
  siteId: site-uuid-here
}

docs {
  Remove a managed site from the current user.
  
  Requires valid b2e_accessToken cookie (automatically sent).
}
