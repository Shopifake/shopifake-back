meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{host}}/api/auth-b2e/logout
  body: none
  auth: none
}

docs {
  Logout the current user.
  
  Clears authentication cookies (b2e_accessToken and b2e_refreshToken).
  Invalidates refresh token in database.
  Cookies are automatically removed from subsequent requests.
}
