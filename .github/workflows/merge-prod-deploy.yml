name: Deploy to Production

on:
  workflow_call:
    inputs:
      lock-path:
        description: 'Relative path of the lock file in the repository'
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy production using lock
        run: |
          echo "TODO: deploy production using ${{ inputs.lock-path }}"
          # scripts/deploy/apply.sh --env prod --lock ${{ inputs.lock-path }}

